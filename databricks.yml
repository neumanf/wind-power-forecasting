# This is a Databricks asset bundle definition for wind_power_forecasting.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: wind_power_forecasting
  uuid: 5ac9618a-880c-472d-b85c-e0a024ed68c5

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-38baafab-bd9e.cloud.databricks.com
    variables:
      catalog: wind_power_forecasting
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-38baafab-bd9e.cloud.databricks.com
      root_path: /Workspace/Users/fabricionewman@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: wind_power_forecasting
      schema: prod
    permissions:
      - user_name: fabricionewman@gmail.com
        level: CAN_MANAGE
